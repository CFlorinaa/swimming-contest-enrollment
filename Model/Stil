package Model;

import java.util.ArrayList;
import java.util.List;

public enum Stil {
    liber, spate, fluture, mixt;

    public static List<String> GetEnumList()
    {
        List<String> stiluri=new ArrayList<>();
        for(Stil s:Stil.values()){
            stiluri.add(s.toString());
        }
        return stiluri;
    }
}
